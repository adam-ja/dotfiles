- clean: ['~']
- link:
    ~/.agignore: misc/agignore
    ~/.zshrc: zsh/.zshrc
    ~/.gitconfig: git/.gitconfig
    ~/.vim: vim/
    ~/.tmux.conf: tmux/.tmux.conf
- shell:
    - [git submodule foreach --recursive git submodule update --init, Installing submodules]
    - [~/dotfiles/install_packages.sh, Installing useful packages]
    - [~/.vim/install.sh, Installing vim (this may take a while)]
    - [~/dotfiles/gsettings.sh, Applying gsettings]
    -
        command: printf "\nRemember to set the default shell to zsh\n\n"
        description: chsh -s `which zsh`
        stdout: true
