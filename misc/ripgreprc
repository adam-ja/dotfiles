# Make searches case-insensitive
--ignore-case

# Don't respect .gitignore and similar VCS ignore files. Only ignore things specified in more general .ignore files
--no-ignore-vcs

# Always show colours even when piping etc
--color=always
